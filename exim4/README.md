# exim4 configuration

These configuration files represent the split exim4 configuration for setting up Maillist.sh.  This documentation is incomplete but I'll circle back and make revisions later.  This isn't guaranteed to work because I invested more time writing the daemon than I did with the configuration.
